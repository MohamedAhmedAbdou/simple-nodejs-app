# Simple Node.js App

A simple Node.js application that is dockerized using a Dockerfile.  
The Docker image is built from the application source and pushed to Docker Hub, allowing it to be run as a container in any environment.

---

## 📌 Features
- Simple Node.js application
- Dockerized using a Dockerfile
- Docker image built locally
- Image pushed to Docker Hub
- Can be run as a container anywhere (local, VM, cloud, Kubernetes)

---

## 🛠 Tech Stack
- Node.js
- Docker
